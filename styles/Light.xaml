<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:local="clr-namespace:Elecciones">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
        <ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options">
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/MaterialDesignColor.Orange.Primary.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <SolidColorBrush x:Key="PrimaryHueLightBrush" Color="{DynamicResource Primary300}" />
            <SolidColorBrush x:Key="PrimaryHueLightForegroundBrush"  Color="{DynamicResource Primary200Foreground}" />
            <SolidColorBrush x:Key="PrimaryHueMidBrush"  Color="{DynamicResource Primary600}" />
            <SolidColorBrush x:Key="PrimaryHueMidForegroundBrush" Color="{DynamicResource Primary500Foreground}" />
            <SolidColorBrush x:Key="PrimaryHueDarkBrush"  Color="{DynamicResource Primary50}" />
            <SolidColorBrush x:Key="PrimaryHueDarkForegroundBrush"  Color="{DynamicResource Primary700Foreground}" />
        </ResourceDictionary>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize"  Value="12"/>
        <Setter Property="Padding"  Value="0"/>
        <Setter Property="Margin"  Value="3"/>
        <Style.Triggers>
            <Trigger Property="Button.Tag" Value="ENTRA">
                <Setter Property="Button.Background" Value="#90e38d" />
            </Trigger>

            <Trigger Property="Button.Tag" Value="SALE">
                <Setter Property="Button.Background" Value="#e38d8d" />

            </Trigger>

            <Trigger Property="Button.Tag" Value="PREPARA">
                <Setter Property="Button.Background" Value="#3ccbf4" />
            </Trigger>

            <Trigger Property="Button.Tag" Value="RESET">
                <Setter Property="Button.Background" Value="#cc0000" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="GroupBox" BasedOn="{StaticResource MaterialDesignGroupBox}">
        <Setter Property="Margin" Value="3"/>
        <Setter Property="Padding" Value="4"/>
    </Style>

    <SolidColorBrush x:Key="ColorFuente"  Color="#4b3d37" />
    <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Foreground" Value="#4b3d37"/>
    </Style>

    <Style TargetType="Label" BasedOn="{StaticResource MaterialDesignLabel}">
        <Setter Property="Foreground" Value="#4b3d37"/>
    </Style>



</ResourceDictionary>